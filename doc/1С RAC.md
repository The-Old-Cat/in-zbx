# 📝 Утилита rac (Remote Administrative Client)

## 📘 Инструкция по применению

**RAC (Remote Administration Console)** — это консольная утилита, позволяющая выполнять административные операции с кластером серверов 1С:Предприятие и всеми его объектами: инфобазами, сессиями, блокировками, пользователями, обработками, лицензиями и прочим.

RAC работает на уровне серверов — **все операции выполняются через RAS (Remote Administration Server)**.

---

## 📌 1. Общий синтаксис RAC

```sh
rac <объект> <команда> [options] [arguments]
```

Просмотр помощи:

```sh
rac help                   — общая помощь
rac <объект> help          — справка по объекту
rac <объект> <команда> -h  — справка по команде
```

---

## 📌 2. Основные объекты RAC

|Объект|Назначение|
|---|---|
|`cluster`|Управление кластером серверов|
|`infobase`|Управление информационными базами|
|`session`|Управление активными сессиями|
|`connection`|Управление соединениями|
|`process`|Управление рабочими процессами (работниками)|
|`lock`|Управление блокировками|
|`user`|Пользователи кластера|
|`event`|Подписки и события|
|`license`|Информация по лицензиям|
|`agent`|Управление RAS / агентом|
|`perf`|Производительность|
|`dump`|Выгрузка объектов|
|`restore`|Восстановление объектов|

---

## 📌 3. Общие параметры

|Параметр|Значение|
|---|---|
|`--cluster <uuid>`|Идентификатор кластера|
|`--infobase <uuid>`|Идентификатор информационной базы|
|`--agent <host:port>`|Адрес RAS (обычно 1545)|
|`--user <login>`|Логин администратора кластера|
|`--password <pwd>`|Пароль|
|`--format <json/xml>`|Формат вывода|
|`--timeout <sec>`|Таймаут операции|

---

## 📌 4. Подключение к агенту RAS

Обычно RAS работает на порту **1545**:

```sh
rac cluster list --agent localhost:1545
```

Если включена авторизация:

```sh
rac cluster list --agent srv01:1545 --user admin --password pass123
```

---

## 📌 5. Управление кластером

### 🔹 Получить список кластеров

```sh
rac cluster list --agent localhost:1545
```

### 🔹 Получить подробную информацию о кластере

```sh
rac cluster info --cluster <uuid> --agent localhost:1545
```

### 🔹 Создать кластер

```sh
rac cluster create --agent localhost:1545
```

### 🔹 Удалить кластер

```sh
rac cluster drop --cluster <uuid> --agent localhost:1545
```

---

## 📌 6. Управление информационными базами

### 🔹 Список инфобаз

```sh
rac infobase summary --cluster <cluster> --agent localhost:1545
```

### 🔹 Подробная информация об инфобазе

```sh
rac infobase info --cluster <cluster> --infobase <uuid>
```

### 🔹 Создание инфобазы

```sh
rac infobase create --cluster <cluster> \
  --name "Test" \
  --descr "Описание" \
  --dbms MSSQLServer \
  --db-server sql01 \
  --db-name Test1C \
  --locale ru \
  --agent localhost:1545
```

### 🔹 Удаление инфобазы

```sh
rac infobase drop --cluster <cluster> --infobase <uuid>
```

### 🔹 Блокировка для обслуживания

```sh
rac infobase update --cluster <cluster> --infobase <uuid> \
  --sessions-deny on \
  --scheduled-jobs-deny on \
  --denied-message "Техническое обслуживание"
```

---

## 📌 7. Управление сессиями

### 🔹 Список активных сессий

```sh
rac session list --cluster <cluster>
```

### 🔹 Информация по конкретной сессии

```sh
rac session info --cluster <cluster> --session <session-id>
```

### 🔹 Завершить сессию

```sh
rac session terminate --cluster <cluster> --session <session-id> --message "Ваш сеанс завершён администратором"
```

### 🔹 Завершить все сессии инфобазы

```sh
rac session terminate --cluster <cluster> --infobase <uuid>
```

---

## 📌 8. Управление соединениями

Список соединений рабочих процессов (workers):

```sh
rac connection list --cluster <cluster>
```

Удалить соединение:

```sh
rac connection terminate --cluster <cluster> --connection <uuid>
```

---

## 📌 9. Управление блокировками (locks)

### 🔹 Список блокировок

```sh
rac lock list --cluster <cluster> --infobase <uuid>
```

### 🔹 Снять блокировки

```sh
rac lock drop --cluster <cluster> --infobase <uuid>
```

---

## 📌 10. Управление рабочими процессами (process)

### 🔹 Список процессов (workers)

```sh
rac process list --cluster <cluster>
```

Получить статистику процесса:

```sh
rac process info --cluster <cluster> --process <uuid>
```

---

## 📌 11. Пользователи кластера

### 🔹 Список пользователей

```sh
rac user list --cluster <cluster>
```

### 🔹 Создать пользователя

```sh
rac user create --cluster <cluster> --name admin2 --password 12345
```

### 🔹 Удалить пользователя

```sh
rac user drop --cluster <cluster> --name admin2
```

---

## 📌 12. Лицензии

Список лицензий:

```sh
rac license list --cluster <cluster>
```

Информация о загрузке лицензий:

```sh
rac license info --cluster <cluster>
```

---

## 📌 13. Производительность

```sh
rac perf list --cluster <cluster>
```

---

## 📌 14. Выгрузка / загрузка инфобазы средствами RAC

### 🔹 Выгрузка (dump)

```sh
rac infobase dump --cluster <cluster> --infobase <uuid> --file C:\temp\ibdump.dt
```

### 🔹 Восстановление (restore)

```sh
rac infobase restore --cluster <cluster> --file C:\temp\ibdump.dt
```

---

## 📌 15. Автоматизация RAC в PowerShell / Linux

Пример PowerShell:

```powershell
$rac = "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe"
$cluster = "<uuid>"

& $rac session list --cluster $cluster --agent "localhost:1545" --format json | ConvertFrom-Json
```

---

## 📌 16. Наиболее частые операции (шпаргалка)

|Задача|Команда|
|---|---|
|Получить список инфобаз|`rac infobase summary`|
|Завершить сеанс|`rac session terminate`|
|Снять блокировки|`rac lock drop`|
|Создать инфобазу|`rac infobase create`|
|Запретить новые сессии|`rac infobase update --sessions-deny on`|
|Получить рабочие процессы|`rac process list`|
|Выгрузка базы|`rac infobase dump`|

---
  > [!important]
>  
> # ✅ Общий принцип вызова rac.exe из PowerShell
> 
> В PowerShell перед `rac.exe` ставится `&`, чтобы выполнить команду:
> 
> ```powershell
> & "путь\к\rac.exe" <объект> <команда> [параметры] [agent_host:port]
> ```
> 
> В конце **всегда указывается RAS** — например `"localhost:1545"`.
> 
> ---
> 
> # 📌 1. Инфобазы (summary list)
> 
> ### Команда:
> 
> ```powershell
> & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" infobase summary list `
>     --cluster=c7313ba1-5875-4498-8184-4a830f12d77f `
>     --cluster-user=admin --cluster-pwd=PassWord `
>     "localhost:1545"
> ```
> 
> ### Что делает:
> 
> * **infobase summary list** — выводит краткий список всех инфобаз на кластере.
> * Показывает:
> 
>   * UUID ИБ
>   * имя
>   * режимы блокировок
>   * количество активных сессий
>   * ограничения
>   * владельца и т. д.
> 
> Для мониторинга Zabbix — это основной источник данных.
> 
> ---
> 
> # 📌 2. Список соединений (connection list)
> 
> ```powershell
> & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" connection `
>     --cluster=c7313ba1-5875-4498-8184-4a830f12d77f list `
>     --cluster-user=admin --cluster-pwd=PassWord `
>     "localhost:1545"
> ```
> 
> ### Что делает:
> 
> * Показывает все **низкоуровневые соединения**:
> 
>   * PID рабочего процесса
>   * путь базы
>   * пользователя
>   * состояние
>   * время простоя
> * Это НЕ сессии, а именно worker’ы → полезно для анализа подвисших процессов.
> 
> ---
> 
> # 📌 3. Список кластеров (cluster list)
> 
> ```powershell
> & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" cluster list
> ```
> 
> ### Что делает:
> 
> * Показывает RAS-узлы и UUID кластеров.
> * Нужна только для получения реального UUID (который ты далее уже используешь).
> 
> ---
> 
> # 📌 4. Список сессий (session list)
> 
> ```powershell
> & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" session `
>     --cluster=c7313ba1-5875-4498-8184-4a830f12d77f list `
>     --cluster-user=admin --cluster-pwd=PassWord `
>     "localhost:1545"
> ```
> 
> ### Что делает:
> 
> * Показывает все **активные пользовательские сессии**.
> * Выдаёт:
> 
>   * user_name
>   * host
>   * infobase
>   * app_id
>   * session_id
>   * dt_issued
>   * blocked_by_locks
>   * длительность сеанса
> 
> Используется для мониторинга нагрузки.
> 
> ---
> 
> # 📌 5. Чтение блокировок (lock list)
> 
> ### Команда:
> 
> ```powershell
> $result = & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" lock `
>     --cluster=c7313ba1-5875-4498-8184-4a830f12d77f list `
>     --cluster-user=admin --cluster-pwd=PassWord `
>     "localhost:1545"
> ```
> 
> ### Что делает:
> 
> * Показывает **активные блокировки объектов 1С**:
> 
>   * режим
>   * тип блокировки
>   * объект
>   * соединение
>   * PID процесса
>   * пользователь
> 
> ---
> 
> # 📌 6. Фильтр результата PowerShell
> 
> ```powershell
> $result | Select-String -Pattern "lock|connection|session|dbpid|object"
> ```
> 
> ### Что делает:
> 
> * Ищет в выводе rac строки, содержащие:
> 
>   * `lock`
>   * `connection`
>   * `session`
>   * `dbpid`
>   * `object`
> 
> То есть **оставляет только ключевые строки**, чтобы проще парсить в Zabbix.
> 
> ---
> 
> # 📌 7. Список рабочих процессов (process list)
> 
> ```powershell
> & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" process `
>     --cluster=c7313ba1-5875-4498-8184-4a830f12d77f list `
>     --cluster-user=admin --cluster-pwd=PassWord `
>     "localhost:1545"
> ```
> 
> ### Что делает:
> 
> Показывает **worker-процессы 1С**:
> 
> * PID в Windows
> * состояние
> * тип (WS, RM, JCP)
> * количество пользователей
> * память
> * процент загрузки
> 
> Очень важно для мониторинга производительности.
> 
> ---
> 
> # 📌 8. Повторная команда lock list (дублируется)
> 
> ```powershell
> & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" lock ...
> ```
> 
> — то же, что выше.
> 
> ---
> 
> # 📌 9. Счётчики производительности (counter list)
> 
> ```powershell
> & "C:\Program Files\1cv8\8.3.27.1786\bin\rac.exe" counter `
>     --cluster=c7313ba1-5875-4498-8184-4a830f12d77f list `
>     --cluster-user=admin --cluster-pwd=PassWord `
>     "localhost:1545"
> ```
> 
> ### Что делает:
> 
> * Показывает **внутренние метрики производительности**:
> 
>   * событий в секунду
>   * ожидания блокировок
>   * время выполнения
>   * deadlock’и
>   * команды СУБД
> 
> Эти показатели — основа мониторинга RAC.
> 
> ---
> 
> 